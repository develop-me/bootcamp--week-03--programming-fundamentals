// Write some code that lists the numbers from 100 to 1 using a `for` loop

// start at 100
// keeping going until equal to 1
// subtract one every time
for (let i = 100; i >= 1; i -= 1) {
    console.log(i);
}
