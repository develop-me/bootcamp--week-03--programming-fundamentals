// Write some code that lists the numbers from 1 to 100 using a `for` loop

// start at 1
// keeping going until equal to 100
// add one every time
for (let i = 1; i <= 100; i += 1) {
    console.log(i);
}
