// Write some code that lists all multiples of 13 up to 1,000 using a `for` loop

// start at 13
// keeping going as long as less than 1000
// add 13 every time
for (let i = 13; i < 1000; i += 13) {
    console.log(i);
}
